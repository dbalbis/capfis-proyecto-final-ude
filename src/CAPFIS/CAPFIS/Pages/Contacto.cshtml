@page
@model CAPFIS.Pages.ContactoModel
@{
    ViewData["Title"] = "Contacto";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <h2 class="mb-4 text-center">Contáctenos</h2>
            <form class="needs-validation" novalidate>
                <div class="mb-3">
                    <label for="nombre" class="form-label">Nombre</label>
                    <input type="text" class="form-control" id="nombre" placeholder="Tu nombre" required>
                    <div class="invalid-feedback">
                        Por favor, ingresa tu nombre.
                    </div>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">Correo electrónico</label>
                    <input type="email" class="form-control" id="email" placeholder="nombre@ejemplo.com" required>
                    <div class="invalid-feedback">
                        Ingresa un correo electrónico válido.
                    </div>
                </div>
                <div class="mb-3">
                    <label for="mensaje" class="form-label">Mensaje</label>
                    <textarea class="form-control" id="mensaje" rows="4" placeholder="Escribe tu mensaje" required></textarea>
                    <div class="invalid-feedback">
                        Por favor, escribe tu mensaje.
                    </div>
                </div>
                <button type="submit" class="btn btn-warning fw-bold w-100">Enviar</button>
            </form>
            <div class="text-center mt-4 d-flex flex-row justify-content-center gap-4 align-items-center">
                <a href="https://www.linkedin.com/in/diego-balbis/" target="_blank" rel="noopener" class="d-flex align-items-center text-decoration-none">
                    <i class="bi bi-linkedin me-2" style="font-size: 2rem; color: #0A66C2;"></i>
                    <span class="text-dark" style="font-size: 0.8rem;">Diego Balbis</span>
                </a>
                <a href="https://www.linkedin.com/in/dayron-mu%C3%B1iz/" target="_blank" rel="noopener" class="d-flex align-items-center text-decoration-none">
                    <i class="bi bi-linkedin me-2" style="font-size: 2rem; color: #0A66C2;"></i>
                    <span class="text-dark" style="font-size: 0.8rem;">Dayron Muñiz</span>
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Bootstrap validación personalizada
        (() => {
            'use strict';
            const forms = document.querySelectorAll('.needs-validation');
            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                    if (!form.checkValidity()) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        })();
    </script>
}
